# short-description: Create the iot-devkit disk image
# long-description: Creates the partitioned disk image that the user
# can directly dd to the sdcard.

part /boot --source iot-devkit --ondisk mmcblkp0 --fstype=msdos --label msdos --active --align 1024

part / --source rootfs --ondisk mmcblkp0 --fstype=ext3 --label platform --align 1024
